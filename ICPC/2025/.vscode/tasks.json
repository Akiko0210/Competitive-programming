{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: build active file",
      "command": "g++",
      "args": [
        "-fcolor-diagnostics",
        "-fansi-escape-codes",
        "--std=c++11",
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.out"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "detail": "Task generated by Debugger."
    }
  ],
  "version": "2.0.0"
}
